# Themis

Role-invariant enforcement service for the khimaira workspace. Defines per-role rules in YAML and evaluates them against tool calls to make role-boundary violations structurally impossible.

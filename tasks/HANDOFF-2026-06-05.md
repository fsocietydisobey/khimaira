# Session Handoff — 2026-06-05 (khimaira-master b401499d → next session)

> Written at wind-down: Joseph hitting personal usage limit → janice-0 transfers jp
> context → ALL sessions close → fresh session starts on the **jeevy account**. This
> doc + the `session_post_handoff` carry continuity. New session: READ THIS FIRST.

## TL;DR for the new session

You are picking up as khimaira-master after a full session-reset onto the jeevy account.
The work is a **khimaira-codebase bug-fix campaign** (roster-coordination substrate bugs)
plus a parked jeevy-account migration. **Relaunch the khimaira roster fresh, read the open
specs in `tasks/`, and resume the bug-fix queue.** Everything below is committed.

## ✅ SHIPPED + COMMITTED today (don't redo)

- **SSE-deafness root fix** (`475c002`) — THE #1 roster-disconnect bug. janice traced it
  (incomplete Phase-B-v1.3 migration: lazy-start `_ensure_subscriber` still spawned the
  stale session-bound `_sse_loop`); fix repoints it onto `_proactive_sse_loop`
  (write_stream, survives compaction). Reviewed audit-grade, 24 tests. **Sessions adopt
  it on restart** — so the NEW roster gets it automatically. Spec:
  `tasks/sse-stale-session-handle-root/`.
- **Concurrency-proxy** (`70f976d` + `2a2b054`) — local Anthropic proxy at :8741,
  **systemd-supervised** (`khimaira-proxy.service`, auto-restart). Cross-session
  concurrency-cap (AIMD N=2-32) + adaptive Retry-After/jitter + FLAG-1 never-crash.
  Absorbs the 32-session server-throttle. `bin/roster` auto-routes through it when alive.
- **Roster-identity class closed live** — Family A + B, all 12 paths (Part F = SSE
  routing/inert-denial; SEPARATE from the SSE-deafness root above).
- **alive-guard** (`d7b4eb7`) — `delete_session` refuses an ACTIVE session (chat-orphaning
  root). + `/khimaira-delete-rosters` command.
- **intake write-access** (`430e1fd`) — Themis IN-INTAKE-1/4 removed (intake = Joseph's
  full-access agents). Approved via quorum-override; **verify-live pending next deploy**.
- **verifier-tier** (`bbfa409`) — ROLE_BUDGET verifier opus→sonnet leak fix.
- **lean-roster** (`3241218`+`af03162`) — tracker/observer default OFF; 11-session default.

## 🔴 BUG-FIX QUEUE (the active work — resume this)

Joseph's pace: **SEQUENTIAL, daemon next.** Reproduce → trace → fix → concurrent-test → B3.

1. **Task-claim atomicity CAS** — `task-f5b53dae5b15`, was IN-PROGRESS on agent-1 when
   sessions closed. **CHECK `git log` / branches for agent-1's WIP first** (it was asked
   to commit WIP). If incomplete, re-dispatch. Spec: `tasks/task-claim-atomicity-gap/`.
   ⭐ GATE-BLOCKING CRUX (analyst+architect converged): the fix must be a TRUE atomic
   check-and-set (per-task `asyncio.Lock` around read→check→write, OR zero `await`
   between status-read and durable rename) — a naive check-then-await-then-rename STILL
   RACES (TOCTOU). And the reproduce-test must be GENUINELY CONCURRENT
   (`asyncio.gather(claim1, claim2)`), not sequential (sequential = false-green). It must
   FAIL against pre-fix code.
2. **Daemon sluggishness** — NEXT after CAS (Joseph's pick: highest-leverage — the slow
   daemon frays everything: 246k-char task dumps, ack timeouts). Profile-first. Prime
   suspect: JSONL O(n) re-reads at ~2000-msg chat files (`load_room` parses whole file
   per call). Spec: `tasks/daemon-sluggishness-review/`.
3. **Restart-identity** — AFTER daemon. Quick wins: title-match nudge + loud-fail-on-dead-
   target + `roster_recovery.py` id-cache audit. DEEP root (restart mints new session-id
   not inheriting membership): **janice-0 is tracing the registration code** (like she did
   SSE) — COORDINATE, don't duplicate. Spec: `tasks/roster-restart-identity-gap/` (has
   live 2026-06-05 evidence appended).
4. **Nudge-liveness** (`tasks/nudge-liveness-false-negative-gap/`) — mostly DOWNSTREAM of
   the SSE fix (now committed); re-evaluate whether still needed after the new roster runs.

## 🅿️ PARKED — jeevy account migration

Joseph wants `~/work` sessions on the jeevy work account (offload personal cap). Set up but
PARKED until "jeevy code is more stable." **Full notes + gotchas + finish-steps:
`~/.claude-jeevy/SETUP-NOTES.md`.** Key: the `.bashrc` auto-switch is DISABLED (broke roster
resume — session index lives in config dir). On-demand jeevy works:
`CLAUDE_CONFIG_DIR=~/.claude-jeevy claude --model sonnet`. A FRESH roster on jeevy sidesteps
the resume issue (resume was the blocker). jeevy lacks `[1m]` context (use plain sonnet/opus).
⚠️ The exposed company API key (was in get-api-key.sh, now using subscription/OAuth) — Joseph
should ROTATE if ever used.

## Key context / decisions

- **Daemon** (port 8740) runs from EDITABLE SOURCE in ~/dev/khimaira; `uv run khimaira
  monitor restart` deploys the working tree. **A restart re-deafens SSE ~40s** then clean
  re-register (B.1 self-heals). Several committed fixes (intake-write verify-live, etc.)
  deploy on next restart.
- **Standing constraints:** master/intake commit directly; route autonomously; ~32
  concurrent sessions across 2 rosters is CORE (not reducible); address sessions by FULL
  UUID never 8-char prefix; verify foundational premises in a THROWAWAY before live (the
  apiKeyHelper + jeevy-resume missteps both came from skipping this).
- **The day's lesson** (in `~/.claude-jeevy/SETUP-NOTES.md`): don't deploy unverified
  auth/config to a live session; verify the mechanism in isolation first.

## First moves for the new session

1. Read this + `session_post_handoff` (SessionStart surfaces it) + the open specs.
2. Relaunch the khimaira roster (fresh). It picks up the SSE fix automatically.
3. Check agent-1's CAS WIP (`git log --oneline -15`, branches) → finish or re-dispatch with
   the atomicity crux bound.
4. Resume the queue: CAS → daemon → restart-identity. Coordinate with janice on the
   restart-identity registration-code trace.
